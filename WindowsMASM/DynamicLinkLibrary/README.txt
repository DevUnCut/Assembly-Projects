The DLL_Skeleton.dll and the DynamicLinkLibrary.lib were obtained by compling this program first then
we are able to use our exported processes that were defined in our DLL_Skeleton.def use the
DLL_Skeleton.dll file in the application in which we want to use those predefined functions
.... in this case we'll be calling MyCustomFunction ! !